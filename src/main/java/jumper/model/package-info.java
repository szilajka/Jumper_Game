/**
 * This package contains the models those are used by the game.
 * <p>
 * It contains the enemy class, the {@link jumper.model.FallingRectangle} class,
 * the user, the {@link jumper.model.Player} class and their superior class,
 * the {@link jumper.model.Rect} class.
 * <p>
 * Also contains a {@link jumper.model.ScoreBoard} class, that is used to store
 * the scores with user names and elapsed time ascending by the score.
 */
package jumper.model;