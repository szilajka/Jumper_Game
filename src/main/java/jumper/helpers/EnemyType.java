package jumper.helpers;

/**
 * An {@code enum} for {@code enemies}.
 * <p>
 * This {@code enum} contains two {@code enemy types}: A {@code Basic} and a {@code Spike} enemy.
 */
public enum EnemyType {
  BasicEnemy, SpikeEnemy
}
